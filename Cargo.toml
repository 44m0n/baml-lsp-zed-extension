[package]
name = "baml_lsp_zed_extension"
version = "0.1.0"
edition = "2024"
description = "Zed language adapter for the BAML CLI language server"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"

[workspace]
